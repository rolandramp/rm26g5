{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "RAG: Run Experiment",
      "type": "python",
      "request": "launch",
      "module": "rag_framework.main",
      "cwd": "${workspaceFolder}",
      "env": {
        "PYTHONPATH": "${workspaceFolder}/src"
      },
      "envFile": "${workspaceFolder}/.env",
      "args": [
        "run-experiment",
        "--config",
        "config/experiment_config.yaml",
        "--output",
        "results"
      ],
      "console": "integratedTerminal",
      "justMyCode": true
    }
  ]
}
